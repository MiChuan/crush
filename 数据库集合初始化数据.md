# 数据库集合初始化数据

> 本文档提供 8 个集合的**可直接导入**的 JSON 数据（含测试菜品/分类/充值档位）。
> 所有字段已与代码逐条核对（核对日期：2026-08-12）。

---

## 一、导入方法

1. 打开**微信开发者工具** → 左上角 **云开发** 控制台
2. 左侧菜单 → **数据库**
3. 点击对应集合名（没有则先点 **+ 新建集合**）
4. 集合内点 **导入** 按钮 → 选择 JSON 文件
5. 冲突处理选 **insert**（或"冲突时替换"），点 **确定**

> 日期字段使用 MongoDB EJSON 格式 `{"$date":"..."}`，控制台导入会识别为 Date 类型。
> 若个别环境导入后日期显示为对象，可忽略或删除该字段（仅影响排序展示，不影响功能）。

## 二、导入顺序（重要）

| 顺序 | 集合 | 必导 | 说明 |
|------|------|------|------|
| 1 | `dishCategory` | ✅ 必导 | 自定义 `_id`（`cat001` 等），供 dish 引用 |
| 2 | `dish` | ✅ 必导 | `categoryId` 引用上面分类的 `_id` |
| 3 | `rechargeOptions` | ✅ 必导 | 充值档位（`status:1` 才会显示） |
| 4 | `admin` | ⭐ 推荐 | 管理员密码 + 店铺设置；不导入则首次进"我的"页设置密码时自动创建 |
| 5 | `tableCode` | 可选 | 建议用管理后台"桌码管理"创建（自动生成小程序码+海报） |
| 6 | `printer` | 可选 | 需真实打印机 SN/KEY，走管理后台绑定，**不要手动导入** |
| 7 | `user` | 跳过 | 用户首次打开小程序自动创建，手动导入无意义 |
| 8 | `order` | 跳过 | 下单/充值自动创建 |

---

## 三、JSON 数据

### 1. `dishCategory`（菜品分类）

```json
[
  {
    "_id": "cat001",
    "name": "招牌热菜",
    "sort": 1,
    "icon": "🍲",
    "createTime": { "$date": "2026-08-01T00:00:00.000Z" }
  },
  {
    "_id": "cat002",
    "name": "饮品小食",
    "sort": 2,
    "icon": "🥤",
    "createTime": { "$date": "2026-08-01T00:00:00.000Z" }
  }
]
```

> 代码读取：`getCategory` 云函数按 `sort` 升序返回；分类名用于点餐页分组。

---

### 2. `dish`（菜品，含测试数据）

```json
[
  {
    "name": "招牌红烧肉",
    "description": "肥而不腻，入口即化，本店招牌",
    "categoryId": "cat001",
    "categoryName": "招牌热菜",
    "image": "",
    "images": [],
    "price": 38,
    "status": 1,
    "sort": 1,
    "tags": [],
    "skus": [
      {
        "id": "sku001",
        "name": "标准份",
        "price": 38,
        "status": 1,
        "sort": 0
      },
      {
        "id": "sku002",
        "name": "大份",
        "price": 52,
        "status": 1,
        "sort": 1
      }
    ],
    "createTime": { "$date": "2026-08-01T00:00:00.000Z" },
    "updateTime": { "$date": "2026-08-01T00:00:00.000Z" }
  },
  {
    "name": "酸辣土豆丝",
    "description": "酸辣开胃，下饭必备",
    "categoryId": "cat001",
    "categoryName": "招牌热菜",
    "image": "",
    "images": [],
    "price": 16,
    "status": 1,
    "sort": 2,
    "tags": [
      {
        "id": "tag001",
        "name": "辣度",
        "type": "single",
        "required": true,
        "options": ["不辣", "微辣", "中辣", "特辣"]
      }
    ],
    "skus": [
      {
        "id": "sku003",
        "name": "标准份",
        "price": 16,
        "status": 1,
        "sort": 0
      }
    ],
    "createTime": { "$date": "2026-08-01T00:00:00.000Z" },
    "updateTime": { "$date": "2026-08-01T00:00:00.000Z" }
  },
  {
    "name": "冰镇柠檬水",
    "description": "新鲜柠檬现榨，清爽解腻",
    "categoryId": "cat002",
    "categoryName": "饮品小食",
    "image": "",
    "images": [],
    "price": 8,
    "status": 1,
    "sort": 1,
    "tags": [
      {
        "id": "tag002",
        "name": "甜度",
        "type": "single",
        "required": true,
        "options": ["三分糖", "五分糖", "七分糖", "全糖"]
      },
      {
        "id": "tag003",
        "name": "加冰",
        "type": "multiple",
        "required": false,
        "options": ["去冰", "少冰", "加冰"]
      }
    ],
    "skus": [
      {
        "id": "sku004",
        "name": "中杯",
        "price": 8,
        "status": 1,
        "sort": 0
      },
      {
        "id": "sku005",
        "name": "大杯",
        "price": 10,
        "status": 1,
        "sort": 1
      }
    ],
    "createTime": { "$date": "2026-08-01T00:00:00.000Z" },
    "updateTime": { "$date": "2026-08-01T00:00:00.000Z" }
  },
  {
    "name": "香脆薯条",
    "description": "金黄酥脆，配番茄酱",
    "categoryId": "cat002",
    "categoryName": "饮品小食",
    "image": "",
    "images": [],
    "price": 12,
    "status": 1,
    "sort": 2,
    "tags": [],
    "skus": [
      {
        "id": "sku006",
        "name": "标准份",
        "price": 12,
        "status": 1,
        "sort": 0
      }
    ],
    "createTime": { "$date": "2026-08-01T00:00:00.000Z" },
    "updateTime": { "$date": "2026-08-01T00:00:00.000Z" }
  }
]
```

**字段说明**：
- `image`/`images`：留空字符串/空数组，前端会显示"暂无图片"占位；后续在管理后台编辑菜品上传图片即可
- `price`：**最低规格价**（展示用），下单按 `skus[].price` 结算
- `skus[].status`：`1` 启用，`0` 停用；至少启用一个
- `tags[].type`：`single` 单选 / `multiple` 多选；`required` 是否必选；`options` 为**字符串数组**
- `status`：`1` 上架 / `0` 下架，点餐页只显示 `status:1`

---

### 3. `rechargeOptions`（充值档位）

```json
[
  {
    "amount": 50,
    "giveAmount": 5,
    "isRecommend": false,
    "status": 1,
    "description": "充50送5",
    "createTime": { "$date": "2026-08-01T00:00:00.000Z" }
  },
  {
    "amount": 100,
    "giveAmount": 15,
    "isRecommend": true,
    "status": 1,
    "description": "充100送15，最受欢迎",
    "createTime": { "$date": "2026-08-01T00:00:00.000Z" }
  },
  {
    "amount": 200,
    "giveAmount": 40,
    "isRecommend": false,
    "status": 1,
    "description": "充200送40",
    "createTime": { "$date": "2026-08-01T00:00:00.000Z" }
  }
]
```

> 代码读取：`recharge` 页按 `where({status:1}).orderBy('amount','asc')` 加载；
> `isRecommend:true` 的档位默认选中。金额单位：**元**。

---

### 4. `admin`（管理员 + 店铺设置）

```json
[
  {
    "password": "123456",
    "shopName": "测试小店",
    "welcomeText": "欢迎光临，请扫码点餐~",
    "createTime": { "$date": "2026-08-01T00:00:00.000Z" },
    "updateTime": { "$date": "2026-08-01T00:00:00.000Z" }
  }
]
```

> ⚠️ **必须只有 1 条记录**（代码取第一条）。
> ⚠️ **导入后请立即在"我的 → 管理后台 → 修改密码"中改掉默认密码 `123456`**。
> `shopName`/`welcomeText` 会显示在点餐页顶部；可在"店铺设置"页修改。

---

### 5. `tableCode`（桌码，可选）

```json
[
  {
    "tableNumber": "A01",
    "qrCodeUrl": "",
    "posterUrl": "",
    "createTime": { "$date": "2026-08-01T00:00:00.000Z" }
  }
]
```

> **建议不用手动导入**：管理后台"桌码管理"创建时会自动调用 `get_code` 云函数
> 生成小程序码（`qrCodeUrl`）和海报（`posterUrl`），手动导入无法生成二维码。
> 手动导入的桌码仍可用于"扫码"场景（scene=桌号），但没有可扫的码和海报。

---

### 6. `printer`（打印机）

```json
[]
```

> **不要手动导入**：`sn`/`key` 必须与打印平台注册的打印机一致，且需调用
> `printManage` 云函数校验。请在管理后台"打印机设置"中绑定。
> 没有打印机可留空，不影响点餐下单。

---

### 7. `user`（用户）

```json
[]
```

> 无需导入：用户首次打开小程序时自动创建（`_openid` 由系统生成，手动导入无效）。

---

### 8. `order`（订单）

```json
[]
```

> 无需导入：点餐/充值订单由 `doBuy` / `recharge` 自动创建。

---

## 四、导入后检查清单

- [ ] 云开发控制台能看到 `dishCategory` 2 条、`dish` 4 条、`rechargeOptions` 3 条、`admin` 1 条
- [ ] 小程序首页 → 点餐页能显示"招牌热菜/饮品小食"两个分类和 4 个菜品
- [ ] 点"酸辣土豆丝"能弹出"辣度"必选标签；点"冰镇柠檬水"能选"甜度+加冰"
- [ ] 充值页能看到 3 个档位，默认选中"充100送15"
- [ ] 管理后台登录密码为 `123456`（改掉它！）
- [ ] 店铺名显示"测试小店"、欢迎词显示"欢迎光临，请扫码点餐~"

## 五、常见问题

| 问题 | 处理 |
|------|------|
| 导入后菜品显示"暂无图片" | 正常，测试数据未传图；管理后台编辑菜品上传即可 |
| 分类下无菜品 | 检查 `dish.status` 是否为 `1`、`categoryId` 是否与分类 `_id` 一致 |
| 充值页空白 | 检查 `rechargeOptions.status` 是否全为 `1` |
| 管理后台登录不了 | `admin` 集合是否有且仅 1 条记录 |
| 点餐页报"获取菜品分类失败" | `dishCategory` 集合是否已创建并导入 |
