# 数据库集合字段说明

> 本说明基于项目代码（`cloudfunctions/` + `miniprogram/`）实际读写逻辑整理，字段与代码一一对应。
> 所有金额字段单位均为**元（Number）**。

## 一、集合总览

| 集合名 | 用途 | 建议权限 | 读写方式 |
|--------|------|---------|---------|
| `user` | 用户信息与余额 | 仅创建者可读写 | 客户端直连 + `getUserList` 云函数 |
| `dish` | 菜品（含规格、标签、图片） | 所有用户可读，仅创建者可写 | 客户端直连 |
| `dishCategory` | 菜品分类 | 所有用户可读，仅创建者可写 | 客户端直连 + `getCategory` 云函数 |
| `order` | 订单（点餐 / 充值） | 仅创建者可读写 | 客户端 + `doBuy`/`pay_success` 云函数 |
| `admin` | 管理员密码 + 店铺设置 | 仅创建者可读写 | 客户端直连 |
| `rechargeOptions` | 充值套餐 | 所有用户可读，仅创建者可写 | 客户端直连 |
| `tableCode` | 桌码 | 所有用户可读，仅创建者可写 | 客户端直连 + `get_code` 云函数 |
| `printer` | 打印机绑定信息 | 仅创建者可读写 | 客户端直连（`doBuy` 云函数读取） |

> `_openid`、`_id` 为云开发系统自动字段，下表中不再单列（`_openid` 在需要显式写入时标注）。

---

## 二、user — 用户

首次进入小程序时自动创建（`app.js` 的 `syncUserRecord`）。

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `_openid` | string | 系统 | 用户 openid（自动写入） |
| `nickName` | string | 否 | 微信昵称（授权后回填） |
| `avatarUrl` | string | 否 | 微信头像 URL（授权后回填） |
| `phoneNumber` | string | 否 | 手机号（`getPhoneNumber` 云函数解密后回填） |
| `balance` | number | 是 | 余额（元），默认 0，扣款/充值用 `db.command.inc` 增减 |
| `createTime` | date | 是 | 创建时间 |

---

## 三、dish — 菜品

管理后台「菜品管理」页维护（`pages/admin/dish/dish.js`）。

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `name` | string | 是 | 菜品名称，≤24 字 |
| `description` | string | 否 | 菜品描述，≤80 字 |
| `categoryId` | string | 是 | 所属分类 ID（`dishCategory._id`） |
| `categoryName` | string | 否 | 分类名称（冗余存储） |
| `image` | string | 是 | 主图（云存储 fileID），等于 `images[0]` |
| `images` | array | 是 | 图片数组（云存储 fileID），1~9 张 |
| `price` | number | 是 | 展示价（元）= 最低启用规格价，下单不以此结算 |
| `skus` | array | 是 | 规格数组，见下方 `skus` 子结构 |
| `tags` | array | 否 | 口味/做法标签组，见下方 `tags` 子结构 |
| `status` | number | 是 | 1-上架 / 0-下架（下架菜品不可下单） |
| `sort` | number | 否 | 排序，升序 |
| `createTime` | date | 是 | 创建时间 |
| `updateTime` | date | 否 | 更新时间 |

**`skus` 子结构**（单规格时自动生成 `{id:'default', name:'默认规格'}`）：

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `id` | string | 是 | 规格 ID，如 `default` / `sku_xxx` |
| `name` | string | 是 | 规格名，如「大份」「小份」 |
| `price` | number | 是 | 规格价格（元） |
| `status` | number | 是 | 1-启用 / 0-停用 |
| `sort` | number | 否 | 排序 |

**`tags` 子结构**（标签组，顾客需按组选择）：

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `name` | string | 是 | 标签组名，如「辣度」「加料」 |
| `type` | string | 是 | `single`-单选 / `multi`-多选 |
| `required` | boolean | 是 | 是否必选 |
| `options` | array | 是 | 选项字符串数组，如 `['微辣','中辣','特辣']` |

> 下单后订单 `goods[].tags` 存的是**已选标签的字符串数组**（如 `['中辣','加香菜']`），与这里的组结构不同。

---

## 四、dishCategory — 菜品分类

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `name` | string | 是 | 分类名，如「招牌菜」「饮品」 |
| `sort` | number | 否 | 排序，升序 |
| `icon` | string | 否 | 图标（emoji），默认 `🍜` |
| `createTime` | date | 否 | 创建时间 |

---

## 五、order — 订单

包含两种订单，用 `type` 区分：`order`（点餐）、`recharge`（充值）。

### 5.1 点餐订单（`type: 'order'`）

由 `doBuy` 云函数事务创建（余额支付）或 `pay` 云函数创建（微信支付）。

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `type` | string | 是 | 固定 `'order'` |
| `goods` | array | 是 | 商品数组，见下方 `goods` 子结构 |
| `totalPrice` | number | 是 | 商品原价合计（元），服务端按规格价重算校验 |
| `finalPrice` | number | 是 | 实付金额（元） |
| `orderType` | string | 是 | `dineIn`-堂食 / `takeOut`-打包 |
| `pay_status` | boolean | 是 | 是否已支付（余额支付下单即 `true`） |
| `payWithBalance` | boolean | 否 | 是否余额支付（用于打印小票显示支付方式） |
| `tableNumber` | string | 是 | 桌号（下单前必须扫桌码） |
| `remark` | string | 否 | 顾客备注，≤120 字 |
| `userNickName` | string | 否 | 下单用户昵称 |
| `userAvatar` | string | 否 | 下单用户头像 |
| `userPhone` | string | 否 | 下单用户手机号（打印小票脱敏显示） |
| `createTime` | date | 是 | 下单时间 |
| `status` | number | 否 | 业务状态（代码中已注释，当前未写入；0-待确认预留） |
| `_openid` | string | 是 | 下单用户 openid（`doBuy` 显式写入） |

**`goods` 子结构**：

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `dishId` | string | 是 | 菜品 ID |
| `dishName` | string | 是 | 菜品名 |
| `dishImage` | string | 否 | 菜品主图 |
| `skuId` | string | 是 | 所选规格 ID |
| `skuName` | string | 是 | 所选规格名 |
| `price` | number | 是 | 规格单价（元） |
| `count` | number | 是 | 数量 |
| `tags` | array | 否 | 已选标签字符串数组 |
| `subtotal` | number | 是 | 小计 = 单价 × 数量 |

### 5.2 充值订单（`type: 'recharge'`）

由充值页（`pages/recharge/recharge.js`）创建，微信支付成功回调后入账。

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `type` | string | 是 | 固定 `'recharge'` |
| `rechargeId` | string | 是 | 对应充值套餐 ID（`rechargeOptions._id`） |
| `amount` | number | 是 | 充值金额（元） |
| `giveAmount` | number | 是 | 赠送金额（元） |
| `totalGet` | number | 是 | 实际到账总额 = amount + giveAmount |
| `pay_status` | boolean | 是 | 支付状态，创建时 `false` |
| `status` | number | 是 | 业务状态，创建时 `0`（待支付） |
| `userNickName` | string | 否 | 用户昵称 |
| `userAvatar` | string | 否 | 用户头像 |
| `userPhone` | string | 否 | 用户手机号 |
| `createTime` | date | 是 | 下单时间 |

---

## 六、admin — 管理员 + 店铺设置

首个管理员在「我的」页首次验证密码时自动创建（仅 `password`、`createTime`、`updateTime`），店铺名/欢迎语由店铺设置保存（`utils/shopSettings.js`）。

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `password` | string | 是 | 管理员密码（明文存储，≥6 位；建议后续改造为哈希存储） |
| `shopName` | string | 否 | 店铺名称 |
| `welcomeText` | string | 否 | 店铺欢迎语 |
| `createTime` | date | 否 | 创建时间 |
| `updateTime` | date | 否 | 更新时间 |

> 注意：该集合只存一条记录（首个管理员创建后，`limit(1).get()` 取第一条）。

---

## 七、rechargeOptions — 充值套餐

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `amount` | number | 是 | 充值金额（元），列表按此升序 |
| `giveAmount` | number | 是 | 赠送金额（元） |
| `isRecommend` | boolean | 否 | 是否推荐（充值页默认选中） |
| `status` | number | 是 | 1-启用 / 0-停用（充值页只展示启用项） |
| `description` | string | 否 | 套餐描述 |

---

## 八、tableCode — 桌码

管理后台创建桌码时生成（`pages/admin/tableCode/tableCode.js` + `get_code` 云函数）。

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `tableNumber` | string | 是 | 桌号（唯一，如「A01」） |
| `qrCodeUrl` | string | 是 | 小程序码图片 fileID（`get_code` 生成，scene 为桌号） |
| `posterUrl` | string | 是 | 海报图 URL（创建时生成，先空后回填） |
| `createTime` | date | 是 | 创建时间 |

---

## 九、printer — 打印机

管理后台绑定打印机时写入（`pages/admin/printer/printer.js`），`doBuy` 云函数下单后读取用于打印小票。

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `sn` | string | 是 | 打印机设备编号（须与打印平台一致） |
| `key` | string | 是 | 打印机绑定码（须与打印平台一致） |
| `name` | string | 否 | 打印机名称，默认 `打印机{sn}` |
| `density` | number | 否 | 打印浓度，默认 6 |
| `printSpeed` | number | 否 | 打印速度，默认 2 |
| `volume` | number | 否 | 蜂鸣音量，默认 3 |
| `createTime` | date | 是 | 绑定时间 |

---

## 十、初始化步骤（微信开发者工具）

1. 云开发控制台 → 数据库 → 创建上述 **8 个集合**。
2. 按上表给每个集合设置权限。
3. 首条数据建议：
   - `admin`：手动插入 `{ password: "你的密码" }`（或首次在小程序里设置）
   - `dishCategory`：插入 1~2 个分类
   - `dish`：插入 1~2 个菜品（含 `skus`）
   - `rechargeOptions`：插入 1~2 个套餐（`amount` + `giveAmount` + `status:1`）
   - `tableCode`、`printer`：在管理后台对应页面创建/绑定
4. 用户数据无需预置，首次打开小程序自动创建。
